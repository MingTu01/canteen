<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.canteen.mapper.OrderMapper">
    <select id="selectByStoreId" resultType="com.example.canteen.entity.Order">
        SELECT * FROM `order` WHERE store_id = #{storeId} ORDER BY created_at DESC
    </select>

    <select id="selectByEmployeeId" resultType="com.example.canteen.entity.Order">
        SELECT * FROM `order` WHERE employee_id = #{employeeId} ORDER BY created_at DESC
    </select>

    <select id="selectByStoreDate" resultType="com.example.canteen.entity.Order">
        SELECT * FROM `order` WHERE store_id = #{storeId} AND date = #{date} ORDER BY created_at DESC
    </select>

    <select id="selectByEmployeeDateMeal" resultType="com.example.canteen.entity.Order">
        SELECT * FROM `order`
        WHERE employee_id = #{employeeId} AND date = #{date} AND meal_type = #{mealType}
          AND status IN (1, 2)
        LIMIT 1
    </select>

    <!-- 取餐码按「门店+日期」收口:防止跨店/跨日碰撞导致错核销;storeId 为 null(超管)时不过滤门店 -->
    <select id="selectByStoreDatePickupCode" resultType="com.example.canteen.entity.Order">
        SELECT * FROM `order`
        WHERE pickup_code = #{pickupCode}
          AND date = #{date}
        <if test="storeId != null">
          AND store_id = #{storeId}
        </if>
        LIMIT 1
    </select>
</mapper>
