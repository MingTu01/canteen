<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.canteen.mapper.EmployeeMapper">
    <select id="selectByStoreId" resultType="com.example.canteen.entity.Employee">
        SELECT * FROM employee WHERE store_id = #{storeId} AND status = 1 AND is_deleted = 0 ORDER BY id
    </select>

    <select id="selectByCardNoAndStore" resultType="com.example.canteen.entity.Employee">
        SELECT * FROM employee
        WHERE card_no = #{cardNo} AND store_id = #{storeId} AND status = 1 AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="countByCardNoExcludeId" resultType="int">
        SELECT COUNT(*) FROM employee
        WHERE card_no = #{cardNo}
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <select id="selectByPhoneAndStore" resultType="com.example.canteen.entity.Employee">
        SELECT * FROM employee
        WHERE phone = #{phone} AND store_id = #{storeId} AND status = 1 AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="selectByNameAndStore" resultType="com.example.canteen.entity.Employee">
        SELECT * FROM employee
        WHERE name = #{name} AND store_id = #{storeId} AND status = 1 AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="selectByPhone" resultType="com.example.canteen.entity.Employee">
        SELECT * FROM employee
        WHERE phone = #{phone} AND status = 1 AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="selectByWxOpenid" resultType="com.example.canteen.entity.Employee">
        SELECT * FROM employee
        WHERE wx_openid = #{wxOpenid} AND status = 1 AND is_deleted = 0
        LIMIT 1
    </select>

    <update id="deductBalance">
        UPDATE employee SET balance = balance - #{amount}, updated_at = NOW()
        WHERE id = #{id} AND balance >= #{amount} AND is_deleted = 0
    </update>

    <update id="addBalance">
        UPDATE employee SET balance = balance + #{amount}, updated_at = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>
